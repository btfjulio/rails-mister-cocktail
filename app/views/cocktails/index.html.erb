<div class="cocktails">
  <% @cocktails.each do |cocktail| %>
    <div class="cocktail">
      <p><%= cocktail.name %></p>
      <p><%= link_to 'See more', cocktail_path(cocktail) %></p>
    </div>
  <% end %>
</div>

<%= link_to 'Create new Cocktail', new_cocktail_path %>